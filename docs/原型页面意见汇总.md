## 原型页面意见汇总

### 页面视觉设计

1. overview页面的运行趋势图，只能视觉上（因此也是定性的）观察过去24小时的运行情况。需要为这个图标添加组件，当鼠标悬停在其中一个柱状时，可以浮现出简要介绍这一时段数据的小组件。
2. overview页面得前端核心工作列表与overview页面得视觉效果不协调。实际开发时可以去掉，或者调整。

### 页面逻辑设计

1. src/main.jsx（line 59） ，即mock memories。这里的数据结构设计为包含id、type等若干字段。目前来看与项目文档中的要求一致，但是没有和后端开发人员协商，实际开发时是否要调整以与后端记忆单元数据结构对齐？
2. 